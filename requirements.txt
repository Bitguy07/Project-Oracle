# Project Oracle — Python Dependencies
# Install: pip install -r requirements.txt

# Google Gemini AI (legacy SDK for compatibility — deprecated but still installable)
google-generativeai==0.8.6  # Pin to last release for stability

# HTTP client (async)
httpx>=0.27.0

# Standard utilities
python-dotenv>=1.0.0      # Local .env file support

# Type hints / data validation
typing-extensions>=4.10.0